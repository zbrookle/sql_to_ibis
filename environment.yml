name: sql_to_ibis
channels:
  - conda-forge
dependencies:
  # required
  - ibis-framework=1.3.0
  - lark-parser=0.9.0
  - python=3.7

  # Test requirements
  - black
  - flake8
  - mypy
  - isort
  - pytest
  - pandas
  - pytest
  - pytest-cov
  - freezegun
